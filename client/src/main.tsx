import React from "react";
import ReactDOM from "react-dom/client";
import HoldemSession from "./pages/holdem/HoldemSession";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <HoldemSession />
  </React.StrictMode>
);
